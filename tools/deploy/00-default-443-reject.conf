# Default 443 server: reject handshake for unknown SNI (no cert sent).
# Include this first (e.g. as 00-default-443-reject) so other 443 servers match by SNI.
# Nginx 1.19.4+
server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    ssl_reject_handshake on;
}
